import {authorized} from './_auth.js';
export default async function handler(req,res){if(!authorized(req))return res.status(401).json({error:'Não autorizado.'});const url=process.env.VITE_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return res.status(503).json({error:'Administração ainda não configurada.'});const response=await fetch(`${url}/rest/v1/interclasse_registrations?select=id,student_name,class_name,modality_id,event_name,division,created_at&order=modality_id.asc,class_name.asc,student_name.asc`,{headers:{apikey:key,Authorization:`Bearer ${key}`}});if(!response.ok)return res.status(502).json({error:'Não foi possível consultar o banco.'});res.setHeader('Cache-Control','no-store');res.status(200).json(await response.json());}
